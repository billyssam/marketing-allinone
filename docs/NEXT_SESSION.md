# 다음 세션 시작점 (2026-08-31 주)

> 마지막 작업: 2026-08-24(월). 테스트 **191 pass** · tsc 클린 · 열린 알림 이슈 0.
> 🔴 **저장소가 PUBLIC이 됐다** — 커밋·이슈·Actions 로그가 전부 공개다. 아래 참조.

## 🚨 첫 일 — 파일럿 D-1

**9월 1일(화) 사장님 1명 초대**로 날짜를 확정했다. 이유·대상·미리 말할 것은
전부 [`docs/PILOT.md`](./PILOT.md)에 있다. **그 문서부터 열 것.**

남은 준비 3가지(전부 9/1 전에):
1. **웹푸시 실기기 검증** — 진짜 폰에서 권한 허용 → 수신까지 한 번도 안 해봤다.
   iOS는 홈화면 추가(PWA) 후에만 푸시가 온다.
2. **초대 자료 1장** — 뭘 하는 도구인지, 첫날 뭘 하면 되는지.
3. **첫 주 운영 리듬** — 며칠째에 뭘 보고 언제 말을 거는지.

초대: `cd backend && npm run preflight` → `npx tsx src/invite-owner.ts <이메일> <상호>`

## 🔴 8/22~8/24 사흘간 자동화가 죽어 있었다 (해결됨)

원인은 **GitHub Actions 예산 $0 + Stop usage**였다. 결제 실패가 아니라 설정이다.
증상이 고약하다 — 워크플로가 **로그 없이** 실패한다(job이 시작조차 못 함).

예산은 결제수단 없이는 **수정도 삭제도 안 된다.** 그래서 **저장소를 공개로 전환**했다
(공개 저장소는 Actions 무제한 무료·예산 체계 미적용). 전환 후 전 워크플로 정상 확인.

**앞으로 같은 증상을 보면**: `gh api repos/OWNER/REPO/check-runs/<job_id>/annotations`

### 공개 저장소라서 지켜야 할 것

- **로그·이슈에 매장 상호를 쓰지 말 것.** `storeLabel()`(`backend/src/mask.ts`)을 쓴다.
  CI는 `MASK_STORE_NAMES=true`라 `[매장 0c59]`로 나가고, 로컬은 실명 그대로다.
- 새 크론을 추가하면 **워크플로 env에 `MASK_STORE_NAMES: 'true'`를 넣을 것.**
- 주간 다이제스트 전문은 이슈가 아니라 **운영자 텔레그램**으로 간다.
- 남은 노출(의도적으로 안 지움): 커밋 메시지 68줄·테스트 픽스처에 `쿵더쿵`·`스타일링룸`.
  **둘 다 우리 테스트 매장이고 실사용자 0명**이라 남의 정보가 아니다.
  진짜 사장님 상호가 커밋에 들어가는 일은 앞으로도 없어야 한다.

## 🔑 사장님이 10분만 쓰면 풀리는 것

**Meta 앱 생성 → App ID·Secret 두 값.** 심사가 **4~6주**라 지금 신청해도 10월 초다.
로드맵의 "9/30 인스타 자동 발행"은 **이미 못 맞춘다** — 파일럿에서는
"자동 발행 없음"을 사장님께 미리 말하는 쪽으로 갔다. 절차는 `docs/meta-review.md`.

## 8/24에 한 것

**크론 주기 조정**(`e35c452`) — 리뷰 크롤 8회/일 → **4회/일(09·14·19·23 KST)**.
청구는 실행시간이 아니라 **작업당 분 단위 올림**이다(133초 → 3분 청구).
8/19에 그걸 놓치고 주기를 올려 월 ~1,080분을 썼다. 지금은 저장소 전체 월 ~720분.
균등 간격이 아닌 이유: 손님은 **방문한 저녁에** 리뷰를 쓴다 → 마지막 회차를 23시로.
계산법은 `docs/ops.md`의 "크론 주기를 바꾸기 전에".

**공개 전환 준비**(`fec721e`) — 위 참조. 히스토리 비밀키 스캔 → 실키 없음 확인 후 전환.
전환 후 **예전 Actions 로그 374개 삭제**(상호가 찍혀 있었다), 다이제스트 이슈 3건 본문 교체.

**초안 정독 도구**(`read-drafts.ts`) — 자동 점검이 전부 초록불이어도 결함은 남는다.
⚠️ 만들면서 **내가 먼저 틀렸다**: `body_html`만 읽어 12건 중 10건이 "본문 0자"로 보였고
대형 결함으로 오인했다. **숏폼은 `body_plain`**이고 제목 null도 정상이다(인스타 캡션엔 제목 없음).
고친 뒤 재확인: 12건 전부 정상(110~3,091자).

## 상태 (바뀐 것 없음)

- 라이브 https://marketing-allinone.vercel.app · Supabase `exmbpietyadkjnunrhka`
- **실사용자 0명** · 자동 발행 0건 · 카톡봇/알림톡/성과지표/결제 미구현
- 매일 도는 것: 데일리 생성(07:30·08:30) · 아침 검증(09:30·10:30) ·
  리뷰 수집(09·14·19·23) · 여정 검증(13:00) · 생존 확인 · 인증 점검

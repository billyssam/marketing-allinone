# 다음 세션 시작점 (2026-08-20 · 파일럿 예정일)

> 마지막 작업: 2026-08-19(수). HEAD `0d59c40` · 테스트 **191 pass** · tsc 클린 · preflight ✅.
>
> ## 🚨 오늘이 파일럿 예정일 — 결정부터
> 섭외가 안 정해졌으면 **날짜를 미룰지 정하는 게 첫 일**이다. 코드는 준비돼 있다.
> 초대 = `cd backend && npm run preflight` → `npx tsx src/invite-owner.ts <이메일> <이름>`.
>
> ## 🔑 사장님이 10분만 쓰면 풀리는 것 (리드타임이 걸린다)
> **Meta 앱 생성 → App ID·Secret 두 값.** 그것만 주시면 인스타 OAuth 흐름을 만들고 시험까지 끝낸다.
> 심사가 **4~6주**라 오늘 신청해도 9월 말이다 — 로드맵의 "9/30까지 인스타 자동 발행"은
> **지금 시작해도 빠듯**하고, 늦어질수록 그냥 못 맞춘다. 절차 전체는 `docs/meta-review.md`.
>
> ## 8/19에 한 것 (커밋 5개)
> **부정 리뷰 지연 15시간 → 2시간**(`1bf5386`)
> - 알림이 **사장님에게 가는 경로가 아예 없었다** — 운영자 텔레그램(미설정)·이슈로만 갔다.
>   → 웹 푸시로 사장님 폰에 직접. 문구에 손님 원문을 넣었다(열게 만드는 건 숫자가 아니라 손님 말).
> - 회차 시간의 대부분이 브라우저 재다운로드였다(설치 24~268초 vs 크롤 60초).
>   → `actions/cache`. 실측 **266초 → 133초**, 아낀 만큼 주기를 3회/일 → **8회/일**(09~23시 2시간).
> - 무료 분 계산: 2시간 간격 8매장 월 1,248분 / 1시간 간격이면 1,988분(초과 위험).
>   **로드맵의 "30분 이하"는 무료 티어로 불가능** — 유료 전환 시 조정.
>
> **Meta 심사 준비**(`e8f4fa4`·`ec60721`)
> - 심사 필수 요건인 **데이터 삭제 콜백** 구현. 인증 헤더가 없고 **서명만이 유일한 방어선**이라
>   뚫리면 누구나 남의 연동을 끊는다 → HMAC 검증 + 단위 6종 + 실호출 검증
>   (올바른 서명 200 / 위조·none우회·깨진입력 400). 운영은 시크릿이 없어 503(의도된 것).
> - **연동 정보만 지운다** — 인스타만 끊었는데 블로그 글까지 사라지면 그게 더 큰 사고.
>
> **여정 검증을 크론으로**(`0d59c40`) — 어제 만든 시뮬레이션이 손으로만 도는 도구였다.
> 매일 13:00 KST 자동 실행, 실패 시 **막힌 지점 요약**을 이슈로, 통과 시 자동 종료.
> `--cleanup-only` + `if: always()`로 검증 매장이 절대 안 남게 했다
> (남으면 다음 아침 크론이 그 매장 글을 만들며 쿼터를 쓴다).
> 무인 실행 실측: **완주 성공 · 3분 남짓**(캐시 적중).
>
> ## 🔜 다음 후보
> 1. 인스타 OAuth 흐름 (App ID·Secret 받으면 즉시)
> 2. 재생성 기능(하루 N회 제한) — 편집만으로 안 되는 요구가 파일럿에서 나올 것
> 3. 실기기 검증 2건: 웹 푸시 권한→수신, 안드로이드 PWA 설치 배너

# (지난) 2026-08-19 시작점

> 마지막 작업: 2026-08-18(화). HEAD `964354c` · 원격 동기화 ✅ · 작업트리 클린.
> **테스트 185 pass** · backend/web tsc 클린 · 프로덕션 빌드 성공 · preflight 전 항목 ✅.
>
> ## 🚨 내일 첫 일 — 날짜 결정부터
> 파일럿 예정일이 **8/20(목)**, 즉 모레다. 섭외가 안 정해졌으면 그 날짜는 의미가 없다.
> **미룰지, 아는 사장님 한 분이라도 이번 주에 넣을지**를 먼저 정하고 그다음을 진행할 것.
> 초대는 `cd backend && npm run preflight` 통과 확인 → `npx tsx src/invite-owner.ts <이메일> <이름>`.
>
> ## 🆕 이제 있는 것 — 사장님 시뮬레이션
> ```bash
> cd backend && npx tsx src/simulate-owner.ts --url=https://marketing-allinone.vercel.app
> ```
> 초대 링크 → 온보딩 → 첫 글 → 붙여넣기 완주 → 리뷰 답글 → 단골 → 주간 리포트까지
> **12단계를 실제 브라우저(아이폰 뷰포트)로 완주**하며 사장님 눈에 보이는 것을 받아 적는다.
> 끝나면 계정·매장을 **반드시 지운다**(`--keep`으로 남길 수 있고 `--url=`로 환경을 바꾼다).
> ⚠️ **로컬로는 초대 링크 경로를 검증할 수 없다** — localhost가 Supabase 허용목록에 없어
>    Site URL 루트로 조용히 바꿔치기된다. 반드시 운영 주소로 돌릴 것.
>
> 마지막 실행(8/18): **막힘 0건** · 온보딩 9탭 · 완료→대시보드 3.1초 · 첫 글 52초 ·
> 붙여넣기 3번에 완주 · 매장 중복 0 · 콘솔 에러 0.
>
> ## 8/18에 한 것 (커밋 8개)
> **무인 4일(8/14~17)이 드러낸 것 4종**(`d102ff3`)
> - 태그 하나가 배열이 아니라서 **매장 하루치 초안이 통째로 0**이 됐다(8/16). 부가 필드는 정규화·비움으로 통과.
> - 품질 이상이 매일 다른 얼굴로 나는데(no-facts→title-prefix→too-short) **아무도 안 고쳤다** → 분량 미달도 자동 보정에 얹음.
> - **생성 규칙과 점검 규칙이 모순**이었다(제목 스타일 'plain'을 점검이 몰라 5채널이 가짜 결함).
> - **스레드는 분량 검사를 아예 안 받고 있었다**(브리프 문구가 달라 파싱 실패) → 전수 테스트로 고정.
>
> **콘텐츠 정직성**(`22d9002`) — 주소 없는 학원에 `#강남수학`·`#역삼수학`.
> 필터가 단문 채널에만 걸려 있었고(반쪽), 광역 지자체만 봐서 구·동을 놓쳤다.
> → 마스터 생성 지점 한 곳으로 옮기고 목록 확장 + 프롬프트 규칙 추가.
>
> **기능 2개**
> - `25e73d1` **초안 편집** — 마음에 안 들 때 할 수 있는 게 아예 없었다.
>   재생성이 아니라 편집을 먼저 만든 이유: 재생성은 Gemini 무료 한도를 쓴다(오늘도 소진돼 lite 강등).
> - `dfa7b87` **아침 알림(웹 푸시)** — 카톡봇 자리를 무료·심사 없이. 만료 구독 정리까지 FCM 410으로 실검증.
>
> **사장님 시뮬레이션**(`757cfd7`·`4b2ae09`·`e9892fc`) — 첫 화면을 사람 눈으로 읽어 결함 5건 + 이후 1건.
> 가입 1분차에 "오늘 몫은 끝났어요" / "10~20초" 안내 vs 실제 52초 /
> **15개 채널이 "완전 자동"으로 표시**(구현 0인 알림톡·쿠팡·광고까지) / 5일 내내 같은 소재 /
> 아이폰에 없는 "주소창 자물쇠" 안내 / 다 했다면서 또 하라는 카드.
>
> ## 🔜 다음 후보
> 1. **시뮬레이션을 매일 크론에** — 지금은 손으로 돌려야 해서 무인 기간에 여정이 깨져도 아무도 모른다.
>    아침 검증이 "초안이 있는가"만 보는 것처럼, 이건 "사장님이 실제로 쓸 수 있는가"를 매일 본다.
> 2. 재생성 기능(하루 N회 제한) — 편집만으로 안 되는 요구가 파일럿에서 나올 것.
> 3. 실기기 검증 2건: 웹 푸시 권한→수신, 안드로이드 PWA 설치 배너.
>
> ## ⚠️ 아직 없는 것 (오해 방지 — DB로 재확인함)
> ```
> 채널 연결 12건 · 인증토큰 있는 것 0건
> 발행 표시 2건 · 실제 발행 URL 남은 것 0건
> ```
> **자동 발행은 한 번도 일어난 적이 없다.** 전부 복사·붙여넣기다.
> 인스타 자동 발행(Meta 심사 4~6주) · 카톡봇 · 알림톡 자동 발송(심사 2주) ·
> 도달·조회 지표 · 결제 — 전부 미구현이고 외부 심사나 사장님 계정이 선행이다.
> **실사용자는 여전히 0명**이다. 시뮬레이션은 "막히는가"를 보는 도구지
> "계속 쓰고 싶은가"는 진짜 사람만 답한다.

# (지난) 2026-08-18 주 시작점

> 마지막 작업: 2026-08-13(수). HEAD `84d704d`. **테스트 164 pass** · backend/web tsc 클린 ·
> 프로덕션 빌드 성공 · **열린 이슈 0** · preflight 전 항목 ✅ · 작업트리 클린.
>
> ## 🚨 돌아오면 가장 먼저 — 파일럿이 코앞이다
> **8/20(목) 파일럿.** 월요일 복귀면 D-3, 화요일이면 D-2다.
> 코드·문서·감시는 다 준비됐고 **남은 블로커는 사장님 섭외 하나뿐**이다.
> 섭외가 안 됐으면 그날 첫 일로 정하거나 날짜를 미루는 결정을 먼저 내릴 것.
> 초대는 `cd backend && npm run preflight` 통과 확인 후
> `npx tsx src/invite-owner.ts <이메일> <이름>` 하나면 끝난다(링크 한 번으로 완주 검증됨).
>
> ## 🔜 무인 4일치(8/14~8/17) 확인 순서
> 1. `gh run list --limit 20` / `gh issue list --state open`
>    — 아침 검증은 이제 **통과하면 스스로 닫히고**, 실패하면 본문에 원인 요약이 들어간다.
>    이슈가 비어 있으면 4일간 정상이었다는 뜻이다.
> 2. **8/17(월) 09:00 주간 다이제스트** — 8/13에 바꾼 "올린 날 N/7" 프레이밍이 처음 실운영으로 나간다.
>    "지나간 글 42" 같은 문장이 사라졌는지, 문구가 카톡에 그대로 붙일 만한지 눈으로 볼 것.
> 3. **8/15(금) 광복절**이 지나간다 — 8/16부터 절기가 안 붙어야 정상(지나간 절기 제외 규칙).
> 4. 리뷰 총량 추이("+N") — 8/13 18시 크론에서 2번째 기록이 쌓였을 것. `/report`에서 확인.
>
> ## 8/13에 한 것 — 전부 "글을 사람처럼 읽어서" 나왔다
> 며칠간 배관만 만졌길래 **실제 나온 초안을 통째로 읽었다.** 6건이 나왔다.
> - **8월에 "동절기 8시까지 영업"**이 4채널에 나갔다(카카오는 "오늘 저녁 8시 라스트오더"로 단정).
>   저장은 원문 유지, **읽는 시점에** 철 지난 계절 조건만 제거(`hoursForNow`). 겨울엔 되살아난다.
> - "21:00에 영업 종료"는 상태 문구지 영업시간이 아니다 → `21:00까지 영업`. 전화 `…0645안내` 정제.
> - **약속대로 매일 올린 사장님을 실패로 적고 있었다** — "56개 준비 / 7개 올림 / 지나간 글 42".
>   분모를 개수에서 **날**로(`올린 날 7/7`). 대시보드 "오늘 할 일"도 끝낼 수 있는 숫자로(0/1+답글).
> - **정보 채널에 사실이 통째로 빠졌다** — 프롬프트 규칙이 있는데도. 지시문으로는 못 막으니
>   생성 후 **코드로 검사·재작성**한다. 감시가 보관된 옛 글을 검사하던 오탐도 같이 제거.
> - **사장님 계정에서 "저희 동네 ○○에서 …하고 있더라구요"**가 나갔다(당근·스레드).
>   단문 프롬프트에 화자 정의가 아예 없었다. 탐지 규칙은 운영 203건에 돌려 **3건 적중·오탐 0** 확인 후 채택.
> - **공개 답글 12건 중 8건이 같은 절**을 품었다 → 인용 창 24→35자, 태그 문구 4종 회전(반복 8→3).
> - 주소 없는 매장에 `#서울공방`이 붙었다 → 주소에 없는 지역 태그 제거.
>
> ## 다음 후보 (섭외가 정리된 뒤)
> 1. **다른 업종으로 한 번 더 읽기** — 지금까지 카페·미용실·공방만 읽었다.
>    병원/학원/헬스장 중 하나로 최악 케이스(플레이스 없음)를 만들어 읽어볼 것. 매번 뭔가 나왔다.
> 2. 사장님이 초안을 **고쳐 쓰고 싶을 때** 할 수 있는 게 없다(재생성·수정 버튼 없음).
>    파일럿에서 확실히 나올 요구인데, Gemini 무료 한도 때문에 남발되면 위험 → 하루 N회 제한 같은 설계 필요.
> 3. GitHub Actions 무료 분: 8매장이면 월 ~1,150/2,000분. 12매장 넘기면 과금 — 그 전에 알 방법.

# (지난) 2026-08-13 시작점

> 마지막 작업: 2026-08-12. HEAD `aed84a0`. **테스트 149 pass**(126→149) ·
> backend/web tsc 클린 · 프로덕션 빌드 성공 · **열린 이슈 0** · preflight 전 항목 ✅.
> 파일럿까지 **8일**.
>
> ## ✅ 어제 물음에 대한 답 — 채널 분할은 크론에서 유지된다
> 아침 준비 검증이 **연속 6회 실패 → 오늘 2회 모두 통과**로 바뀌었다.
> ```
> 08-09 11:35 · 12:16   failure
> 08-10 11:41 · 12:26   failure
> 08-11 11:35 · 12:19   failure   ← 채널 분할 수정 전
> 08-12 12:04 · 12:38   success   ← 수정 후 첫 무인 검증
> ```
> 데일리 크론 소요 = 매장당 73초(2매장 146초). 8매장이면 ~10분, 타임아웃 20분 안에 든다.
>
> ## 🔴 8/12에 잡은 것 — 전부 "안 세어본 것"에서 나왔다
> 1. **플레이스 연결 안내가 43업종 중 32곳에서 안 떴다**(`fa07035`).
>    `saleModes.includes('offline')`로 걸렀는데 saleModes는 *어떻게 파는가*지 매장 유무가 아니다.
>    미용실·헬스장·병원·학원·펜션은 전부 `['reservation']` → offline false.
>    정작 그 안내를 코드 주석이 "콘텐츠 품질 격차의 최대 원인"이라 불렀고 파일럿 매장 하나가 미용실이다.
>    → `hasPlacePage(biz)` 신설(기본 '있다', 진짜 없는 쪽만 예외) + `pickNudge()`로 판정 분리.
> 2. **조사 5곳** — `shared/korean.ts`를 만들어두고 안 쓴 자리. 설정 "메뉴**이** 없어요",
>    단골 문자 "곧 **크리스마스이라**"(12절기 중 7개 오류), 답글 "달라진 **쿵더쿵로**".
> 3. **절기 선택 3중 결함** — 첫 매칭 `break`라 연중 이른 절기가 늘 이김(5/8이 어린이날),
>    "곧"인데 지나간 절기를 잡음, 같은 해 절댓값이라 12/28→1/1이 361일(연말·연초 전멸).
> 4. **리뷰 화면이 같은 것을 두 숫자로**(`1069e62`) — 부정 40(요약) vs 33(탭).
>    요약 KPI는 이미 전체 count로 고쳐져 있었고 **탭만 100건 목록에서 다시 세고 있었다**(반쪽 수정).
> 5. **주간 '새 리뷰'가 부풀었다** — `crawled_at` 기준이라 첫 크롤의 과거 리뷰가 이번 주로 잡힘.
>    스타일링룸 실측 **12건 → 실제 2건**. 이 숫자가 월요일 카톡으로 나간다. `posted_at` 우선으로 정정.
>
> ## 🔜 내일 첫 순서
> 1. `gh run list --limit 10` / `gh issue list --state open`
>    — **아침 준비 검증이 이틀 연속 통과인지**(오늘이 첫날이었다. 1회로 판정하지 말 것).
> 2. 오늘 배포(`1069e62`)가 크론 산출물에 실효했는지 — 월요일 다이제스트 전에
>    `npx tsx src/weekly-digest.ts`로 "새 리뷰" 수치가 여전히 정상인지 한 번 더.
> 3. 리뷰 총량 추이("+N")가 2회 이상 쌓였는지.
>
> ## 🎯 남은 것 (8/20까지 8일)
> - **사장님 섭외·초대**가 유일한 실제 블로커. 코드·문서는 준비됨(`docs/pilot-kit.md`).
>   초대 직전 `cd backend && npm run preflight` 하나만 돌리면 된다.
> - 사장님 외부 액션(선택): 커스텀 SMTP(자가가입 열려면·도메인 필요) · 소셜 로그인 ·
>   Gemini 유료(8매장 초과 시) · 알림톡 credential · 운영자 텔레그램 토큰
> - 실기기 미확인 2건: `naver.me` 단축링크 펼침(실링크는 사장님만 있음) ·
>   안드로이드 PWA 설치 배너(헤드리스는 `beforeinstallprompt`가 안 뜬다)
>
> ## ⚠️ 새로 알게 된 한도
> - **GitHub Actions 무료 분**: 2매장 기준 11분/일(월 ~340/2,000). 8매장이면 월 ~1,150분.
>   Gemini 무료 한도(~8매장)와 얼추 같은 지점에서 걸린다. 그 위로는 둘 다 유료.
> - `check-auth-config.ts`를 로컬에서 돌릴 땐 **`OWNER_SIGNUP_MODE=invite`를 줘야** 한다.
>   안 주면 가짜 🔴이 뜬다(크론은 repo variable로 준다). `docs/ops.md`에 반영함.

# (지난) 2026-08-11 화요일 시작점

> 마지막 작업: 2026-08-06 (13커밋). HEAD `17d583f`. git 클린·전부 푸시·
> **테스트 122→126 pass**·양쪽 tsc 클린·웹 빌드 성공·열린 이슈 0.
> 🔑 Supabase 계정 = **billysir@naver.com** · 프로젝트 `exmbpietyadkjnunrhka`
> 매장 2곳 = 쿵더쿵(카페) · 스타일링룸(미용실, 8/6 리허설로 생성 — 매일 2업종 자동 회귀검증용)
> 리허설 계정 `rehearsal-hair@gmail.com` / `Rehearsal!2345` (지우려면 이 계정만 삭제하면 cascade)

## 🔜 화요일 첫 순서

1. `gh run list --limit 15` / `gh issue list --state open`
   — **금·토·일·월 4일치 크론 결과를 한 번에 본다.** 이슈가 있으면 그것부터.
2. **8/10(월) 09:00 주간 다이제스트가 처음 자동 발화**했는지 확인(이슈로 와 있어야 함).
   내용에 매장 2곳이 다 들어갔는지, 카톡에 그대로 붙일 만한지.
3. **리뷰 총량 추이가 처음 나오는지** — 주 1회 기록이라 화요일이면 2회 이상 쌓여
   "네이버 리뷰 280 / 지난 기록보다 +N"이 떠야 한다(`/report` 또는 다이제스트).
4. 품질 점검이 4일간 오탐을 냈는지 확인 — 냈다면 규칙을 완화할 것.

## 🎯 8/20 파일럿까지 남은 것

- 실제 사장님 섭외·초대(`docs/pilot-kit.md` 그대로 쓰면 됨. 초대는 링크 한 번으로 완주 검증됨)
- 선택: 커스텀 SMTP(자가가입 열려면) · 소셜 로그인 · Gemini 유료(8매장 넘으면)
- 미착수(파일럿엔 불필요): 성과 도달·조회(채널 API 심사) · 카톡봇 진입점 · 결제

## 2026-08-06 한 것 (13커밋)

- 🔴 **초대 링크가 로그인으로 튕기던 것**(`9257a85`) — 리허설이 잡았다. Supabase 메일 링크는
  PKCE code가 아니라 **URL 해시**로 토큰을 주는데 콜백이 "code 없으면 error"였다.
  안 잡았으면 8/20 아침에 사장님 전원이 못 들어왔다.
- 🔴 **부정 리뷰가 아무에게도 안 가던 것**(`a5f3f07`) — 대시보드 최상단 경고 + 이슈 폴백,
  답글 달면 알림 중단.
- 🛡️ **콘텐츠 품질 자동 점검**(`279e70e`·`87afe52`) — 사람 없는 날의 눈. 오탐 2건 자체 수정.
- 🥇 **파일럿 초대 리허설 전 경로 완주**(미용실로) · **실기기 검증 완료**(사용자 확인)
- 조사 처리(`3305f0f`) · 붙여넣기 순서(`72e15b2`) · 단골 문자(`bb49584`) ·
  리뷰 총량 추이(`3fe9f1f`) · 43업종 전수 테스트(`17d583f`) · pilot-kit 최신화(`5890f17`)

## (지난) 2026-08-06 이전 기록

> 마지막 작업: 2026-08-05. HEAD = 아래 로그 참조(14커밋). git 클린·전부 푸시·
> **테스트 101 pass**·백엔드/웹 tsc 클린·프로덕션 빌드 성공·`/api/health` ok:true·
> **열린 이슈 0건**·크론 전부 success.
> 라이브: https://marketing-allinone.vercel.app · DB: Supabase `exmbpietyadkjnunrhka`
> 🔑 Supabase 계정 = **billysir@naver.com** (billy5285@gmail.com 아님 — 그쪽엔 이름만 같은
> 옛 프로젝트가 paused로 있고 🚫Resume 금지)
> 로컬: `cd web && npm run dev` (3500) 또는 preview `marketing-all-in-one`

## ✅ 파일럿 블로커 3건 — 전부 정리됨 (8/5)

1. 리다이렉트 허용목록 → 사장님이 처리, `check-auth-config` ✅ 통과 확인.
2. 4채널 enum SQL → 처리, **8채널 생성·`/prepare`까지 실검증**.
3. 확인메일 OFF → **대시보드에 토글이 없어 불가**(문서와 UI 불일치) →
   **초대 운영 모드로 확정**(repo variable `OWNER_SIGNUP_MODE=invite`).
   자가가입을 열려면 커스텀 SMTP를 붙이고 이 플래그를 지우면 된다.

## 🔜 내일 첫 순서

1. `gh run list --limit 8` / `gh issue list --state open`(비어야 정상).
2. **오늘 넣은 3가지가 크론 산출물에 실효했는지 DB로 확인**:
   ① 단문 채널 사실 반영(플레이스·카톡·밴드에 가격/영업시간이 1~2개 들어갔는지)
   ② 8채널 분량(브리프 목표 대비)
   ③ 대시보드 "오늘 하나만 한다면"이 어제와 다른 채널을 고르는지(로테이션)
3. 그다음 후보는 아래 "다음 후보" 참고.

## ⚠️ 지켜볼 것

- ~~리뷰 크론이 11분 19초 걸린 날(8/5). playwright install이 10분 32초~~
  → **2026-08-19 해결**: `actions/cache`로 브라우저 캐싱. 실측 회차 266초 → **133초**
  (브라우저 설치 184초 → 47초, 그 47초는 캐시가 못 담는 OS 라이브러리 설치분).
  아낀 만큼 크롤 주기를 3회/일 → **8회/일**(09~23시 2시간)로 올려 부정 리뷰 지연을 15시간 → 2시간으로.
- demo 매장은 **8채널 연결 상태**로 둔다(매일 전 채널 회귀 감지용). 단문 재작성은
  채널 수와 무관하게 1회 호출이라 Gemini 비용은 늘지 않는다.

## 🚨 파일럿(8/20) 블로커 — 사장님 액션 3건

> 셋 다 Supabase 대시보드 작업. 코드는 전부 준비돼 있고 설정만 바뀌면 즉시 살아난다.
> 매일 05:30 `auth-config-check`가 1·2번을 실측해 이슈로 울린다.

### 1. 🔴 리다이렉트 허용목록 (가장 급함 — 인증 전체가 깨져 있음)

Supabase가 **허용목록에 없는 주소를 에러 없이 Site URL로 바꿔치기**한다.
실측: 운영 주소를 넘겼는데 `redirect_to=http://localhost:3000`이 돌아왔다.
→ 비밀번호 재설정 메일·가입 확인 메일·향후 카카오/구글 OAuth 콜백이 **전부 죽은 링크**.
앱은 200을 뱉고 헬스체크도 통과하므로 화면으론 절대 안 보인다.

**Authentication → URL Configuration**
- Site URL = `https://marketing-allinone.vercel.app`
- Redirect URLs에 `https://marketing-allinone.vercel.app/**` 추가

확인: `cd backend && npx tsx src/check-auth-config.ts` → 허용목록 항목 ✅

### 2. 🔴 메일 도달 (사장님 자가가입이 막혀 있음)

내장 메일 서비스 = 프로젝트 전체 **시간당 2통** + 팀 외 주소 발송 거부.
확인메일 ON이라 사장님이 가입 버튼을 누르면 `429 over_email_send_rate_limit` →
**계정조차 생성되지 않는다**(실측).

- 근본 해결: 커스텀 SMTP(Resend — CARTON 홈페이지에서 이미 쓰는 그것) 연결 후
  repo variable `CUSTOM_SMTP_CONFIGURED=true`
- **그때까지 파일럿은 초대 방식으로 진행 가능(이미 구현·E2E 검증 완료)**:
  `cd backend && npx tsx src/invite-owner.ts <이메일> <이름>` → 카톡 안내문 출력

### 3. 4채널 DB enum (SQL)

플레이스·당근·밴드·카카오채널 4채널이 **코드는 배포 완료, DB enum만 대기** 중입니다.
SQL 실행 즉시 재배포 없이 다음 크론부터 자동 생성됩니다.

- 운영 프로젝트 ref = `exmbpietyadkjnunrhka` (`.env.local` URL이 단일 진실)
- ⛔ paused된 "marketing-allinone"은 7/9에 버린 옛 것 — **Resume 금지**(과금)
- 두 프로젝트 **이름이 동일**하므로 구분은 ref로만
- 계정 찾기: Gmail `from:supabase` / `chrome://password-manager/passwords`

```sql
alter type post_channel add value if not exists 'naver_place';
alter type post_channel add value if not exists 'danggeun';
alter type post_channel add value if not exists 'naver_band';
alter type post_channel add value if not exists 'kakao_channel';
```
확인: `cd backend && npx tsx src/generate-daily.ts --force` → "저장 스킵" 없이 채널 전부 생성되면 성공.

## 세션 시작 5분 점검

1. `gh run list --limit 10` / `gh issue list --state open` (비어야 정상)
2. 어제 넣은 수정이 크론 산출물에 반영됐는지 (제목 프리픽스 0·반복시어 0·threads 200자+)

## 2026-08-04 한 것 (인증 + 범용성)

- `d854fab` **비밀번호 재설정 신설** — 아예 없었음. 소셜 로그인이 꺼져 있어 이메일이
  유일 진입로라, 비번 잊은 사장님은 영영 못 들어오는 상태였다.
- `8f7401b` **인증 경로가 운영에서 통째로 깨져 있던 것 발견**(위 블로커 1·2) +
  우회로 `invite-owner.ts`(메일 없이 계정 개통, E2E 4/4) + 매일 05:30 감시 워크플로.
  pilot-kit의 "링크에서 이메일로 가입" = 못 지키는 약속이라 초대 절차로 교체.
- `6710ef3` **비음식 업종에서 가격이 하나도 안 잡히던 것 수정** — 검증이 카페 한 곳뿐이었다.
  미용실·헬스장·네일샵 홈에는 메뉴 링크가 0개고 가격은 별도 `/price` 탭에 있었다.
- **범용성 E2E 실측**: 준오헤어 크롤 16건 → 블로그 초안 2,573자,
  **실제 시술·가격 12건 인용**(남성컷 35,000·셋팅펌 250,000),
  어휘도 "메뉴" 0회 / "시술" 3회로 업종에 맞게 나옴. 쿵더쿵 회귀 없음.
- `5bb06bd` 🔴 **미용실 칭찬이 부정 알림으로 가던 것** — `머리카락`·`위생`이 강한부정에
  단독으로 있어서 "머리카락이 상했는데 살려주셔서 감사해요"가 negative로 뒤집혔다.
  실리뷰 49건 재측정: 카페 9/9·미용실 10/10·네일 10/10·헬스 10/10·정형외과 9/10.
- `00ee911` 제목 few-shot 예시 6종이 전부 카페였던 것 → 업종별 4종으로 분리.
- **교훈**: 범용성 결함은 한 곳에 안 몰려 있다. 크롤 → 감정분석 → 프롬프트 예시까지
  층마다 음식 기준이 박혀 있었고, 각 층을 **실제 비음식 업체로 실측해야만** 드러났다.
  새 기능을 넣을 때마다 "카페 말고 미용실이면?"을 한 번씩 돌려볼 것.

## 2026-08-03 한 것 (5커밋 + 검증 3건)

- `07caf3c` 일주일 실물 검증 후속 4종: 반복시어 문턱 1회로·프리픽스 첫어절 제약·
  threads brief 모순 수정·**조용한 실패 로그화 → Gemini 503 발견 → 재시도 추가**
- `eda2215` 미답 리뷰 우선 로딩(오래된 미답이 목록 밖으로 밀리던 문제)
- `70a157f` 단골 목록 40명씩 + 더 보기(212장 동시 렌더 해소)
- `3d2d240` 글 보관함 카운트·필터 전체 기준(200건 초과 시 숫자 틀어짐)
- `bea1018` 웰컴 드래프트 "지연" 오탐 방지(실측 36초 vs 임계 45초)
- 검증만 한 것: 채널 연결→생성→해제→중단 E2E · **멀티매장 크론(4매장 181초 실패0)** ·
  첫날 여정 모바일 완주

## 다음 후보 (우선순위)

1. **SQL 실행 후 4채널 실물 검증** — 플레이스 소식·당근 글 품질, /prepare 흐름.
2. **주간 리포트** — 사장님이 "이번 주 이렇게 했어요"를 체감할 요약(성과 체감 = 파일럿 유지율).
3. **파일럿 초대 리허설** — pilot-kit.md 문구로 실제 초대 → 가입 → 첫 주 흐름 점검.
4. 스마트플레이스 답글 딥링크 실계정 검증(파일럿 첫날 항목).

## 외부 게이트 (사장님이 필요할 때)

카카오/구글 OAuth(`docs/oauth-setup.md`) · Gemini 유료(6~8매장 초과 시) · 확인메일 OFF ·
알림톡 credential(현재 sms: 1탭으로 대체 가능) · 운영자 텔레그램 토큰

## 주의사항 (실제로 겪은 것)

- 🚫 옛 Supabase `oricgerprwgijnowjokn` resume 금지
- PS5.1: 커밋 메시지에 따옴표·괄호 있으면 `git commit -F <파일>`
- **Windows `pkill -f`는 next 프로세스를 못 죽인다** → `Get-NetTCPConnection -LocalPort N | Stop-Process`
  (안 죽이면 옛 빌드가 서빙돼 "수정이 반영 안 된 것처럼" 보임)
- **판정은 항상 DB 원문으로** — /prepare 미리보기는 500자 클램프, 생성은 30초+ 걸림.
  화면 스냅샷 하나로 "실패"라 결론짓지 말 것(오늘 두 번 오판했다가 재확인으로 정정)
- 단일 관측이 다른 증거와 모순되면 결론 전에 재확인(`select industries` 0행 → 실제 45행)
- Gemini flash 무료 20회/일 — 실측 많이 하면 소진돼 lite 폴백. 중요 검증은 오전에
- 테스트 계정은 검증 후 반드시 정리
- **DB 컬럼·enum을 추측하지 말 것**(8/12에 세 번 틀렸다): 리뷰는 `author`가 아니라
  `author_display`, `source`는 `naver_place`, posts 채널 enum은 `naver_blog`가 아니라 `blog`
  (레지스트리 id와 다르며 `channelIdOfPost`가 매핑한다). `select('*').limit(1)`로 먼저 확인.
- **결함을 고치기 전에 얼마나 흔한지 먼저 재라**(8/13에 두 번 도움받았다):
  · 단골 문자가 똑같이 나오길래 결함인 줄 알았는데 20명에 20종 — 뽑은 3건이 우연히 겹친 것이었다.
  · 화자 탐지 규칙을 운영 203건에 돌려 적중·오탐을 보고 나서 좁혔다(넓게 잡았으면 정상 글을 계속 흔들었다).
- **저장분은 원문, 판정은 읽는 시점.** 계절·시점에 따라 달라지는 값(영업시간의 동절기 조건 등)을
  크롤 시점에 지우면 그 정보가 **영영 안 돌아온다**. `normalizeHours`(텍스트) / `hoursForNow`(계절) 분리.
- 시각에 의존하는 함수는 **기준 시각을 주입 가능하게** 만들 것. 안 그러면 테스트가 계절 따라 통과했다 실패했다 한다.
- **검증 도구가 거짓말하는 게 결함을 놓치는 것보다 나쁘다**(8/18에 두 번 겪었다):
  · 시뮬레이터가 `input[type="text"]`로 못 찾았는데 "입력함"이라고 로그를 남겼다 → 값을 되읽어 확인.
  · 콜드스타트로 3.1~7.2초 흔들리는데 5초 고정 임계라 매번 다른 판정 → 되돌릴 수 없는 선만 막힘으로.
- **안내 시간은 가장 느린 실측에 맞춘다.** "10~20초"라 써놓고 실제 52초면 사장님은 고장난 줄 안다.
- **`automation`(계획)과 `status`(현재)를 반드시 함께 볼 것.** 안 합치면 미구현 채널이
  "완전 자동"으로 사장님 화면에 뜬다 — 실제로 15개가 그랬다.
- `reviews.crawled_at`에는 DB 기본값 `now()`가 있다 — 직접 insert하면 전부 "오늘 수집"이 된다.
- **검증용 데이터는 만든 세션에서 반드시 지운다**(`src/probe-nudge.ts --drop`).
  안 지우면 다음 아침 크론이 그 매장을 물고 Gemini 쿼터를 쓴다.

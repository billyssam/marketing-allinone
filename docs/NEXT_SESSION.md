# 다음 세션 시작점 (2026-09-09 이후)

> 🔴 저장소는 **PUBLIC**이다 — 로그·이슈에 상호 금지, 새 워크플로엔 `MASK_STORE_NAMES: 'true'`.
> 상태는 문서 말고 **DB에서 직접** 확인할 것: `cd backend && npx tsx src/who-is-in.ts`

## 🔴 첫 일 — 3주를 잃은 착각을 다시 밟지 말 것

**"사장님이 Meta App ID를 주시면"이라고 3주 동안 적어 왔다. 틀렸다.**

| | 운영자(우리) | 고객(사장님) |
|---|---|---|
| Meta 앱 등록·심사 | **한 번** | 안 함 (존재를 모름) |
| Google Cloud · 알리고 계정 | **한 번** | 안 함 |
| 인스타·페북·스레드·구글 연결 | — | **버튼 눌러 로그인** |
| 스마트스토어 키 · 카카오 채널 | — | 자기 것만 입력 |

App ID 는 **우리 앱**이다. 그걸 고객 블로커로 오해해서, 열 수 있는 채널을 3주간 안 열었다.
전체 표와 절차 = **`docs/OPERATOR_SETUP.md`**.

⚠️ 고객 화면에 우리 사정(App ID·심사·알리고)을 적지 않는다.
`test-channels-screen.ts` 가 운영자 용어 누출을 판정으로 막는다.

## 🔑 지금 막힌 것 딱 하나 — Meta 앱

```
https://developers.facebook.com/apps
```

만들고 Vercel 에 `META_APP_ID`·`META_APP_SECRET` 만 넣으면,
인스타·페북·스레드가 화면에서 **저절로** `아직 못 엽니다` → `한 번 연결하면 자동` 으로 올라간다
(`readinessOf()` 가 환경변수를 보고 판단 — 코드 수정 불필요).
심사 4~6주라 **넣는 날이 곧 10월 파일럿 가능 여부**를 가른다. 자료는 `docs/meta-review.md` 에 완비.

그다음 내가 할 일: OAuth 콜백 라우트(`/api/connect/[channel]`) 구현 — **아직 없다.**
화면의 `연결하기` 버튼은 그 주소를 가리키지만 라우트가 없어 지금 누르면 404다.

## 9/9에 한 것

**① 네이버 원클릭 발행 — 이 제품이 처음으로 "올려주는 도구"가 됐다**
`Pre-Service-Blog-Instagram` 에서 놀고 있던 확장(7/1 E2E 검증 완료)을 가져와 일반화했다.
`extension/inject.js` 하나가 **플레이스·밴드·당근·카카오채널·구글비즈** 다섯을 덮고,
블로그만 SmartEditor 라 `content.js` 로 따로. 원클릭 채널 **2 → 6개**.
- 웹↔확장은 `postMessage` 다리(`bridge.js`) — 확장 ID 가 기기마다 달라 runtime 방식은 못 쓴다
- 못 찾으면 **조용히 넘어가지 않는다** — "글은 복사해 뒀습니다"를 화면에 띄운다
- ⚠️ **Chrome 이 확장을 싣는 것과 네이버 주입은 자동 검증 불가**(헤드리스 확장 미지원,
  헤드 모드는 이 환경에서 `spawn UNKNOWN`). 사람이 `extension/README.md` 대로 확인해야 한다

**② 채널 연결 화면 재설계**(`/channels`)
우리 분류(유입/판매/재방문/광고) → **고객이 할 행동**으로: 한 번 연결하면 자동 / 바로 씁니다 /
키를 넣으면 열립니다 / 아직 못 엽니다. 각 채널에 **실적(만든 글·올림)을 같은 줄에** 붙여
"연결됨인데 0건"이 바로 보이게 했다. 판정 = `readiness.ts` 한 곳.

**③ 온보딩 알림 단계**(5→6단계) — 7/1 MVP 에 있었는데 3주간 안 만들었던 것.
대시보드 카드로만 권유하다 실사용자가 9일치 글을 한 번도 못 받았다.

**④ 웰컴 초안 실패를 DB에 남긴다** — 여정이 한 번 막혔는데 원인을 못 봤다(콘솔로만 흘렀다).
이제 `activity_log.welcome_draft_failed` 에 이유가 남고 여정 검증이 `원인: …` 까지 보고한다.

## 내가 오늘 만든 결함(전부 수정함) — 같은 걸 또 하지 말 것

- 고객 화면에 **운영자 용어**를 노출(App ID·심사) → 판정으로 막음
- 알림톡 입력에 **우리 알리고 계정**을 물어봄 → 고객 것(카카오 채널)만 남김
- 알림 상자 **두 개 중복**(내가 넣은 것 + PushToggle) → 상태 아는 곳 하나가 강조까지
- 채널 19개가 전부 **"아직 준비 중이에요"** → 19개 전부 진짜 이유
- 저장 버튼이 **"확인하는 중"**인데 아무것도 확인 안 함 → "저장하는 중"
- 글자 크기 9종 → 6종(`19px`·`16px` 는 내가 새로 들여온 값)

## 자동으로 도는 것 (10개 워크플로)

- **화면 검증 35항목** — push 마다 + 매일 15:00. 가짜 대량 데이터로 실제 브라우저 완주
  (`test-reply-flow` · `test-regulars-report` · `test-extension-bridge` · `test-channels-screen` · `test-dashboard`)
- **사장님 감시** 08:30 — 신규 가입은 그날, 7일 이탈은 신호로(상세는 텔레그램)
- 데일리 생성 07:30·08:30 · 아침 검증 09:30·10:30 · 리뷰 수집 09·14·19·23 · 여정 검증 13:00(업종 교대)

## 진단 도구

```bash
cd backend && npx tsx src/who-is-in.ts            # 지금 누가 들어와 있나
```
```bash
cd backend && npx tsx src/audit-channels.ts       # 채널이 정말 연결되는가
```
```bash
cd backend && npx tsx src/read-screen.ts --path=channels --email=<계정>   # 화면 정독
```
⚠️ `--path=` 는 **슬래시 없이**. Git Bash 가 `/channels` 를 윈도우 경로로 바꾼다.

## 상태

- 라이브 https://marketing-allinone.vercel.app · Supabase `exmbpietyadkjnunrhka`
- 실사용자 **1명**(본디, 8/26 가입 후 미방문) · 자동 발행 **0건** · 결제 미구현
- 파일럿 **10/1(목)** — `docs/PILOT.md` · 초대 자료 `docs/OWNER_CHECKLIST.md`
